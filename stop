#!/usr/bin/env bash
# Stop all threat detection services (Postgres, Engine, Dashboard).

set -e
cd "$(dirname "$0")"

if ! command -v docker &>/dev/null; then
  echo "Error: Docker is not installed or not in PATH." >&2
  exit 1
fi

echo "Stopping threat detection services..."
sudo docker compose down

echo "All services stopped."
